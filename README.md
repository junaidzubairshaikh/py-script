# py-script


```
# Requirement
1. Make sure the chrome version 84 is installed.(Otherwise, you have to install chrome driver that match your chrome version)
2. Python is installed  

```

```

# Changes in automate-digi file
1. login("login-url","txtStudentId","txtNewPassword","btnLogin") -> login url, id's of username, password and button

```